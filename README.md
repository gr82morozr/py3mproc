# Python3 Multiprocessing Workflow 

- json config workflow


For install :
> pip install py3mproc
For upgrade:
> pip install py3mproc --upgrade -vvv  --no-cache-dir

For PyPi upload:
> python setup.py sdist upload -r pypi

